{
  "name": "perpetual-cabbage",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/perpetual-cabbage",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/perpetual-cabbage",
        "outputPath": "dist/apps/perpetual-cabbage"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/perpetual-cabbage"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "perpetual-cabbage:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "perpetual-cabbage:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "perpetual-cabbage:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "perpetual-cabbage:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/perpetual-cabbage/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/perpetual-cabbage/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
