import type { Meta } from '@storybook/react';
import { Input } from '@watheia/base-ui';

const Story: Meta<typeof Input> = {
  component: Input,
  title: '@watheia/base-ui.atoms.input',
};
export default Story;

export const Primary = {
  args: {},
};
