import type { Meta } from '@storybook/react';
import { Container } from '@watheia/base-ui';

const Story: Meta<typeof Container> = {
  component: Container,
  title: '@watheia/base-ui.atoms.container',
};
export default Story;

export const Primary = {
  args: {},
};
