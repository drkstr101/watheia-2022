import type { Meta } from '@storybook/react';
import { Text } from '@watheia/base-ui';

const Story: Meta<typeof Text> = {
  component: Text,
  title: '@watheia/base-ui.atoms.text',
};
export default Story;

export const Primary = {
  args: {},
};
