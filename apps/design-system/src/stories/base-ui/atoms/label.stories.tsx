import type { Meta } from '@storybook/react';
import { Label } from '@watheia/base-ui';

const Story: Meta<typeof Label> = {
  component: Label,
  title: 'watheia.base-ui/atoms/label',
};
export default Story;

export const Primary = {
  args: {},
};
