import type { Meta } from '@storybook/react';
import { NavBar } from '@watheia/base-ui';

const Story: Meta<typeof NavBar> = {
  component: NavBar,
  title: 'watheia.base-ui/organisms/nav-bar',
};
export default Story;

export const Primary = {
  args: {},
};
