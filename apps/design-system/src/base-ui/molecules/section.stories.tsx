import type { Meta } from '@storybook/react';
import { Section } from '@watheia/base-ui';

const Story: Meta<typeof Section> = {
  component: Section,
  title: 'watheia.base-ui/molecules/section',
};
export default Story;

export const Primary = {
  args: {},
};
