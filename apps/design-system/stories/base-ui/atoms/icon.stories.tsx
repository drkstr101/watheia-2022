import type { Meta } from '@storybook/react';
import { Icon } from '@watheia/base-ui';

const Story: Meta<typeof Icon> = {
  component: Icon,
  title: 'watheia.base-ui/atoms/icon',
};
export default Story;

export const Primary = {
  args: {},
};
