import styles from './button.module.css';

const { button, solid, outline, link, white, neutral, primary, secondary, accent } = styles;

const theme = { button, solid, link, outline, white, neutral, primary, secondary, accent };

export default theme;
